---
import { getLang, getAlternatePath, t, localizedUrl } from '@/utils/i18n';

const lang = getLang(Astro.url);
const currentPath = Astro.url.pathname;
const alternatePath = getAlternatePath(currentPath);
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <div class="footer__inner">
      <div class="footer__brand">
        <a href={localizedUrl('/', lang)} class="footer__logo">
          <img src="/logo-datashake-white.svg" alt="AI datashake" class="footer__logo-img" width="612" height="95" />
        </a>
        <p class="footer__tagline">
          {t(lang, 'footerTagline')}
        </p>
      </div>

      <nav class="footer__nav" aria-label="Navigation">
        <div class="footer__nav-group">
          <h4 class="footer__nav-title">{t(lang, 'navigation')}</h4>
          <ul>
            <li><a href={localizedUrl('/', lang)}>{t(lang, 'home')}</a></li>
            <li><a href={localizedUrl('/blog/', lang)}>{t(lang, 'blog')}</a></li>
          </ul>
        </div>
        <div class="footer__nav-group">
          <h4 class="footer__nav-title">datashake</h4>
          <ul>
            <li><a href="https://www.datashake.fr" target="_blank" rel="noopener">datashake.fr</a></li>
          </ul>
        </div>
        <div class="footer__nav-group">
          <h4 class="footer__nav-title">Language</h4>
          <ul>
            <li>
              <a href={lang === 'fr' ? currentPath : alternatePath} class:list={{ 'footer__lang-active': lang === 'fr' }}>FR</a>
              {' / '}
              <a href={lang === 'en' ? currentPath : alternatePath} class:list={{ 'footer__lang-active': lang === 'en' }}>EN</a>
            </li>
          </ul>
        </div>
      </nav>
    </div>

    <div class="footer__bottom">
      <p>&copy; {currentYear} datashake. {t(lang, 'allRightsReserved')}</p>
    </div>
  </div>
</footer>

<style>
  /* Fond noir DA #101010, textes en blanc attenue */
  .footer {
    background-color: var(--ds-black);
    color: var(--text-on-dark-body);
    padding: var(--spacing-3xl) 0 var(--spacing-xl);
    margin-top: var(--spacing-4xl);
  }

  .footer__inner {
    display: flex;
    justify-content: space-between;
    gap: var(--spacing-3xl);
    padding-bottom: var(--spacing-2xl);
    /* token bordure/filet-sombre : blanc 30 % */
    border-bottom: 1px solid var(--rule-dark);
  }

  .footer__brand {
    max-width: 360px;
  }

  .footer__logo {
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    margin-bottom: var(--spacing-md);
  }

  .footer__logo:hover {
    opacity: 1;
  }

  /* Logo blanc de la charte : aucun filtre, aucune deformation */
  .footer__logo-img {
    display: block;
    height: 20px;
    width: auto;
  }

  .footer__tagline {
    font-size: var(--font-size-sm);
    line-height: 1.6;
    color: var(--text-on-dark-body);
    margin: 0;
  }

  .footer__nav {
    display: flex;
    gap: var(--spacing-3xl);
  }

  .footer__nav-title {
    font-family: var(--font-family-title);
    font-size: var(--font-size-xs);
    font-weight: 500;
    color: var(--text-on-dark);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--spacing-md);
  }

  .footer__nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer__nav li {
    margin-bottom: var(--spacing-sm);
  }

  .footer__nav a {
    color: var(--text-on-dark-body);
    font-size: var(--font-size-sm);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .footer__nav a:hover {
    color: var(--text-on-dark);
    opacity: 1;
  }

  .footer__lang-active {
    color: var(--text-on-dark) !important;
    font-weight: 500;
  }

  .footer__bottom {
    padding-top: var(--spacing-xl);
    font-size: var(--font-size-xs);
    color: var(--text-on-dark-muted);
    text-align: center;
  }

  .footer__bottom p {
    margin: 0;
  }

  @media (max-width: 768px) {
    .footer__inner {
      flex-direction: column;
      gap: var(--spacing-2xl);
    }

    .footer__nav {
      gap: var(--spacing-2xl);
      flex-wrap: wrap;
    }
  }
</style>
