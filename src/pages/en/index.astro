---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Carousel from '@/components/Carousel.astro';
import ArticleCard from '@/components/ArticleCard.astro';
import { localizedUrl, t } from '@/utils/i18n';
import { getCollection } from 'astro:content';

const lang = 'en';
const allPosts = (await getCollection('blog-en', ({ data }) => !data.draft))
  .sort((a, b) => new Date(b.data.date).valueOf() - new Date(a.data.date).valueOf());

const latestPosts = allPosts.slice(0, 6);
const categories = [...new Set(allPosts.map((post) => post.data.category))];
---

<BaseLayout
  title={t(lang, 'homeTitle')}
  description={t(lang, 'homeDescription')}
>
  <!-- Hero : fond beige, couleur signature datashake -->
  <section class="hero">
    <div class="container">
      <div class="hero__content">
        <p class="eyebrow hero__label">{t(lang, 'heroLabel')}</p>
        <h1 class="hero__title">
          {t(lang, 'heroTitle1')} {t(lang, 'heroTitle2')}
        </h1>
        <p class="hero__description">
          {t(lang, 'heroDescription')}
        </p>
        <div class="hero__actions">
          <a href={localizedUrl('/blog/', lang)} class="btn btn--primary">
            {t(lang, 'exploreBlog')}
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M4 11h11.2l-4.6-4.6L12 5l7 7-7 7-1.4-1.4 4.6-4.6H4z" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Latest articles carousel -->
  <Carousel title={t(lang, 'latestArticles')}>
    {latestPosts.map((post) => (
      <ArticleCard
        title={post.data.title}
        description={post.data.description}
        date={post.data.date}
        category={post.data.category}
        slug={post.slug}
      />
    ))}
  </Carousel>

  <!-- Categories -->
  {categories.length > 0 && (
    <section class="section section--beige">
      <div class="container">
        <h2>{t(lang, 'categories')}</h2>
        <p class="section__intro">{t(lang, 'exploreByTopic')}</p>
        <div class="categories-grid">
          {categories.map((category) => {
            const categorySlug = category.toLowerCase().replace(/\s+/g, '-').normalize('NFD').replace(/[\u0300-\u036f]/g, '');
            const count = allPosts.filter((p) => p.data.category === category).length;
            return (
              <a href={localizedUrl(`/category/${categorySlug}/`, lang)} class="category-card">
                <h3 class="category-card__title">{category}</h3>
                <p class="category-card__count">
                  {count} {count > 1 ? t(lang, 'articles') : t(lang, 'article')}
                </p>
              </a>
            );
          })}
        </div>
      </div>
    </section>
  )}
</BaseLayout>

<style>
  /* --- Hero --- */
  .hero {
    background-color: var(--ds-beige);
    padding: var(--spacing-4xl) 0;
  }

  .hero__content {
    max-width: 860px;
  }

  .hero__label {
    margin-bottom: var(--spacing-lg);
  }

  /* Titre : une seule graisse, noir 100 % */
  .hero__title {
    font-size: var(--font-size-5xl);
    font-weight: var(--font-weight-title);
    letter-spacing: -0.03em;
    line-height: 1.08;
    max-width: 20ch;
    margin-bottom: var(--spacing-lg);
  }

  .hero__description {
    font-size: var(--font-size-lg);
    color: var(--text-body);
    line-height: 1.7;
    max-width: 62ch;
    margin-bottom: var(--spacing-xl);
  }

  .hero__actions {
    display: flex;
    gap: var(--spacing-md);
  }

  .section__intro {
    color: var(--text-body);
    margin-bottom: var(--spacing-xl);
  }

  /* --- Grille de categories : cartes blanches bordees --- */
  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: var(--spacing-lg);
  }

  .category-card {
    display: block;
    padding: var(--spacing-xl);
    background: var(--ds-white);
    border: 1px solid var(--border-card);
    border-radius: var(--radius-card);
    text-decoration: none;
    transition: border-color var(--transition-base), transform var(--transition-base);
  }

  .category-card:hover {
    border-color: var(--ds-black);
    transform: translateY(-2px);
    opacity: 1;
  }

  .category-card__title {
    font-size: var(--font-size-xl);
    color: var(--text-strong);
    margin-bottom: var(--spacing-xs);
  }

  .category-card__count {
    font-size: var(--font-size-sm);
    color: var(--text-muted);
    margin: 0;
  }

  @media (max-width: 768px) {
    .hero {
      padding: var(--spacing-2xl) 0;
    }

    .hero__title {
      font-size: var(--font-size-3xl);
      max-width: none;
    }
  }
</style>
